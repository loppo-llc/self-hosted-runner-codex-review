# Codex PR Review 設定
# すべてのフィールドはオプション。省略時はデフォルト値が使われる。

# 使用モデル（デフォルト: Codex CLI のデフォルトモデル）
# model: gpt-5.4

# プロジェクト固有のレビュー追加指示（レビュープロンプトに追記される）
# extra_prompt: |
#   このプロジェクトは Go 1.24 + gRPC を使用している。
#   並行処理のデータ競合とコネクションリークに特に注意すること。
#
# INFO レベルを抑制したい場合:
# extra_prompt: |
#   INFO レベルの指摘は出力しないこと。CRITICAL と WARNING のみ報告せよ。
